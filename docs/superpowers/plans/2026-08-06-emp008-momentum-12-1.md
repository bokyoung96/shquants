# EMP008 12-1 Momentum Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add `momentum_12_1m`, `origin_12_1m`, an all-factor diagnostic set, and five-quantile subplot artifacts.

**Architecture:** Extend the existing factor registry and builder pipeline without changing current strategy memberships. Reuse `evaluate_factor_quantiles` and plot its validated cumulative-return table during artifact publication.

**Tech Stack:** Python 3.12, pandas, matplotlib, pytest, Ruff

---

### Task 1: Register and calculate 12-1 momentum

**Files:**
- Modify: `backtesting/strategies/emp008/factor_builders.py`
- Modify: `backtesting/strategies/emp008/factor_registry.py`
- Test: `tests/strategies/test_emp008_factor_registry.py`
- Test: `tests/scripts/test_run_emp008_full.py`

- [ ] Add failing assertions for `FactorId.MOMENTUM_12_1M`, the formula `P(t-1)/P(t-12)-1`, and `origin_12_1m` membership.
- [ ] Run the two focused tests and confirm failures identify the missing ID/builder/set.
- [ ] Add `build_momentum_12_1m`, `FactorSetId.ORIGIN_12_1M`, and its Origin policy metadata.
- [ ] Run the focused tests and confirm they pass.

### Task 2: Add the all-factor diagnostics set

**Files:**
- Modify: `backtesting/strategies/emp008/factor_registry.py`
- Modify: `tests/strategies/test_emp008_factor_registry.py`

- [ ] Add a failing test that `all_factors` contains every independent factor once in registry order and has no MFBT neutralization.
- [ ] Add `FactorSetId.ALL_FACTORS` with the union membership and FY0 snapshot allowance.
- [ ] Run registry and data-loading tests.

### Task 3: Publish quintile subplot images

**Files:**
- Modify: `backtesting/strategies/emp008/factor_quantiles.py`
- Modify: `tests/strategies/test_emp008_factor_quantiles.py`
- Modify: `backtesting/strategies/emp008/README.md`

- [ ] Add a failing output test expecting equal-weight and market-cap-weight subplot PNGs.
- [ ] Implement a plotting helper that consumes validated `cumulative_returns`, creates one axis per factor, and plots Q1-Q5 plus the directional spread.
- [ ] Add both paths to the output payload and document the command/artifacts.
- [ ] Run focused tests and Ruff.

### Task 4: Execute the full catalog diagnostic

**Files:**
- Output: `results/emp008_factor_quantiles/all_factors/`

- [ ] Run `python -m backtesting.strategies.emp008.run_factor_quantiles --factor-set all_factors --quantiles 5 --start 2020-01-31 --end 2026-06-30 --parquet-dir parquet --output-dir results/emp008_factor_quantiles/all_factors`.
- [ ] Confirm all tabular artifacts and both PNG files are non-empty.
- [ ] Visually inspect both PNGs for populated, readable subplots.
- [ ] Commit with Lore trailers, push, and fast-forward remote main.

